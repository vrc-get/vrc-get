"use client"

import {Typography} from "@material-tailwind/react";

export default function Page() {
	return (
		<div className={"p-4"}>
			<Typography className={"whitespace-normal"}>Editing Settings is not supported yet. Please use `vrc-get` cli or official VCC instead for now.</Typography>
		</div>
	);
}
